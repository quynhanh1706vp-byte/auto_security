/* VSP Dashboard – Compact 1080p layout */
.vsp-dashboard-1080 {
  max-width: 1320px;
  margin: 0 auto;
  padding: 20px 32px 24px;
  font-size: 13px;
}
.vsp-row {
  display: flex;
  gap: 16px;
  margin-bottom: 16px;
}
.vsp-col {
  background: radial-gradient(circle at top left, rgba(39, 73, 148, 0.55), rgba(2, 6, 23, 0.95));
  border-radius: 18px;
  padding: 14px 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.45);
}
.vsp-row-top .vsp-col.title-zone {
  flex: 1.1;
  background: linear-gradient(135deg, rgba(22, 101, 52, 0.8), rgba(5, 46, 22, 0.95));
}
.vsp-row-top .vsp-col.kpi-zone {
  flex: 2;
}
.vsp-main-title {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 6px;
}
.vsp-main-subtitle {
  font-size: 12px;
  opacity: 0.85;
  margin-bottom: 10px;
}
.vsp-main-meta span {
  display: inline-block;
  font-size: 11px;
  opacity: 0.8;
  margin-right: 12px;
}
.vsp-kpi-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
}
.vsp-kpi {
  border-radius: 14px;
  padding: 6px 8px;
  background: rgba(15, 23, 42, 0.92);
  box-shadow: 0 6px 14px rgba(0, 0, 0, 0.55);
}
.vsp-kpi .label {
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  opacity: 0.8;
}
.vsp-kpi .value {
  font-size: 18px;
  font-weight: 600;
  margin: 2px 0;
}
.vsp-kpi .meta {
  font-size: 10px;
  opacity: 0.7;
}
.vsp-kpi.sev-critical { border-left: 3px solid #fb7185; }
.vsp-kpi.sev-high     { border-left: 3px solid #f97316; }
.vsp-kpi.sev-medium   { border-left: 3px solid #eab308; }
.vsp-kpi.sev-low      { border-left: 3px solid #22c55e; }

.vsp-row-middle .vsp-col {
  flex: 1;
}
.chart-holder {
  position: relative;
  height: 210px;
}
.vsp-row-bottom .vsp-col {
  flex: 1;
}

/* Mini tables */
.vsp-mini-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 11px;
}
.vsp-mini-table th,
.vsp-mini-table td {
  padding: 4px 6px;
  border-bottom: 1px solid rgba(148, 163, 184, 0.25);
}
.vsp-mini-table th {
  text-align: left;
  font-weight: 500;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  opacity: 0.8;
}
.vsp-mini-table tbody tr:hover {
  background: rgba(15, 23, 42, 0.85);
}

/* Tắt scrollbar dọc trong dashboard – fit 1 màn hình */
#tab-dashboard {
  overflow-y: hidden;
}


.vsp-chart-note {
  font-size: 11px;
  opacity: 0.7;
  margin: 2px 0 8px 0;
}

/* ---- Layout cho 4 tab phụ (Runs, Data, Settings, Rules) ---- */
#tab-runs,
#tab-data,
#tab-settings,
#tab-rules {
  padding: 24px 40px 40px;
  max-width: 1320px;
  margin: 0 auto;
}

.vsp-section-title {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 4px;
}

.vsp-section-subtitle {
  font-size: 12px;
  opacity: 0.8;
  margin-bottom: 12px;
}

/* Bảng lớn */
.vsp-table-wrapper {
  border-radius: 18px;
  background: radial-gradient(circle at top left, rgba(30,64,175,0.45), rgba(15,23,42,0.95));
  box-shadow: 0 12px 25px rgba(0,0,0,0.55);
  padding: 12px 16px;
  max-height: 440px;         /* tránh tràn màn, có scroll riêng bên trong nếu dài */
  overflow-y: auto;
}

.vsp-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}

.vsp-table th,
.vsp-table td {
  padding: 6px 8px;
  border-bottom: 1px solid rgba(148,163,184,0.25);
}

.vsp-table th {
  text-align: left;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  opacity: 0.8;
}

.vsp-table tbody tr:hover {
  background: rgba(15,23,42,0.9);
}

/* Cards + pill cho Settings */
.vsp-settings-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12px;
  margin-bottom: 16px;
}

.vsp-card {
  background: radial-gradient(circle at top left, rgba(34,197,94,0.5), rgba(5,46,22,0.96));
  border-radius: 16px;
  padding: 10px 12px;
  box-shadow: 0 10px 22px rgba(0,0,0,0.6);
}

.vsp-card-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  opacity: 0.8;
}

.vsp-card-value {
  font-size: 13px;
  margin-top: 4px;
  word-break: break-all;
}

.vsp-subsection-title {
  font-size: 14px;
  font-weight: 500;
  margin: 8px 0;
}

.vsp-pill-row {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.vsp-pill {
  border-radius: 999px;
  padding: 3px 8px;
  background: rgba(15,23,42,0.9);
  border: 1px solid rgba(56,189,248,0.9);
  font-size: 11px;
}

/* Empty / error state */
.vsp-empty,
.vsp-error {
  border-radius: 16px;
  padding: 16px 20px;
  background: rgba(15,23,42,0.95);
  box-shadow: 0 10px 20px rgba(0,0,0,0.6);
  font-size: 12px;
}
.vsp-error {
  border-left: 3px solid #fb7185;
}

/* Hiển thị mỗi lần 1 tab – theo class .active */
.tab-pane {
  display: none;
}
.tab-pane.active {
  display: block;
}

/* Force: mỗi lần chỉ hiển thị 1 tab theo .active */
.tab-pane {
  display: none !important;
}
.tab-pane.active {
  display: block !important;
}

/* ===== VSP 2025 – DASHBOARD COMPACT LAYOUT (RUNTIME V2) ===== */

.vsp-dashboard-root {
  height: calc(100vh - 140px); /* trừ header/top bar */
  display: flex;
  flex-direction: column;
  gap: 12px;
  overflow: hidden;           /* không cuộn body */
}

/* 10 KPI = 2 hàng x 5 card */
.vsp-kpi-row {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 8px;
  margin-bottom: 4px;
}

.vsp-kpi-card {
  background: rgba(8, 24, 48, 0.95);
  border-radius: 12px;
  padding: 8px 12px;
  font-size: 11px;
  line-height: 1.3;
  border: 1px solid rgba(60, 120, 200, 0.35);
}

.vsp-kpi-label {
  opacity: 0.7;
  margin-bottom: 4px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.vsp-kpi-value {
  font-size: 15px;
  font-weight: 600;
}

/* màu cho bucket severity (match tone hiện tại) */
.vsp-kpi-card.sev-critical { border-color: #ff4b6a; }
.vsp-kpi-card.sev-high     { border-color: #ff9b42; }
.vsp-kpi-card.sev-medium   { border-color: #ffda6a; }
.vsp-kpi-card.sev-low      { border-color: #4caf50; }
.vsp-kpi-card.sev-info     { border-color: #5bc0de; }

/* Zone phía dưới: 2 cột, auto fill 2 hàng (tối đa 4 card) */
.vsp-dashboard-bottom {
  flex: 1;
  min-height: 0; /* cho phép con flex-scroll */
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  grid-auto-rows: minmax(0, 1fr);
  gap: 12px;
}

/* Card chung cho Trend / Top risk / Noisy / CVE / By tool */
.vsp-card {
  background: radial-gradient(circle at top left, rgba(25, 55, 109, 0.9), rgba(5, 13, 32, 0.98));
  border-radius: 14px;
  padding: 10px 14px;
  border: 1px solid rgba(80, 140, 220, 0.4);
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.vsp-card.full {
  grid-column: span 2; /* Trend chiếm 2 cột */
}

.vsp-card-title {
  font-size: 12px;
  font-weight: 600;
  margin-bottom: 6px;
}

/* Bảng bên trong card: chỉ card cuộn, không cuộn trang */
.vsp-table-wrapper {
  flex: 1;
  min-height: 0;
  margin-top: 4px;
  overflow-y: auto;
  scrollbar-width: thin;
}

.vsp-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 11px;
}

.vsp-table thead tr {
  background: rgba(9, 28, 60, 0.9);
  position: sticky;
  top: 0;
}

.vsp-table th,
.vsp-table td {
  padding: 4px 6px;
  border-bottom: 1px solid rgba(30, 60, 110, 0.8);
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.vsp-table th {
  font-weight: 500;
  text-align: left;
  opacity: 0.9;
}

.vsp-table td.text-right,
.vsp-table th.text-right {
  text-align: right;
}

/* Loading + error nhỏ gọn, không phá layout */
.vsp-loading,
.vsp-error {
  font-size: 12px;
  opacity: 0.8;
  padding: 12px;
}
.vsp-error { color: #ff6b81; }

/* Grid tổng cho Dashboard (đề phòng) */
.vsp-dashboard-grid {
  display: flex;
  flex-direction: column;
  height: 100%;
  gap: 8px;
}

/* ---- CHART ZONE (3 card ngang) ---- */
.vsp-dashboard-charts {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12px;
  min-height: 220px;
}

.vsp-dashboard-charts .vsp-card {
  min-height: 0;
}

.vsp-dashboard-charts canvas {
  width: 100% !important;
  height: 180px !important;
}

/* 3 bảng phía dưới: chia 3 cột */
.vsp-dashboard-bottom {
  flex: 1;
  min-height: 0;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  grid-auto-rows: minmax(0, 1fr);
  gap: 12px;
}

/* Tinh lại bố cục 3 card phía dưới cho đều, header dính trên – table fill */
.vsp-dashboard-bottom .vsp-card {
  display: flex;
  flex-direction: column;
  min-height: 0;
}
.vsp-dashboard-bottom .vsp-card .vsp-card-title {
  flex: 0 0 auto;
}
.vsp-dashboard-bottom .vsp-card .vsp-table-wrapper {
  flex: 1 1 auto;
  min-height: 0;
}

/* ==== Responsive cho màn hình hẹp (chia nửa) ==== */

/* Mặc định: 3 cột chart, 3 cột bảng phía dưới */
.vsp-dashboard-charts {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}
.vsp-dashboard-bottom {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}

/* Canvas không quá cao để tránh kéo dài dọc */
.vsp-card canvas {
  max-height: 260px;
}

/* Khi chia đôi màn (~<= 1700px) thì co lại cho đỡ dài */
@media (max-width: 1700px) {
  .vsp-dashboard-charts {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .vsp-dashboard-bottom {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .vsp-dashboard-bottom .vsp-card:last-child {
    grid-column: 1 / -1; /* card “By tool – severity buckets” chiếm full hàng dưới */
  }
  .vsp-card canvas {
    max-height: 210px;
  }
  .vsp-card {
    padding-top: 14px;
    padding-bottom: 14px;
  }
}

/* Donut severity luôn tròn và nằm giữa card */
#vsp-chart-severity {
  display: block;
  margin: 0 auto;
  max-height: 260px;
}

/* FIX: Donut severity luôn là hình tròn 260x260, căn giữa card */
#vsp-chart-severity {
  display: block;
  margin: 0 auto;
  width: 260px !important;
  height: 260px !important;
  max-width: 260px !important;
  max-height: 260px !important;
}

/* FIX: Donut severity luôn là hình tròn 260x260, căn giữa card */
#vsp-chart-severity {
  display: block;
  margin: 0 auto;
  width: 260px !important;
  height: 260px !important;
  max-width: 260px !important;
  max-height: 260px !important;
}

/* === OVERRIDE: không giới hạn chung tất cả canvas nữa === */
.vsp-card canvas {
  max-height: none !important;
}

/* Trend + Bar: chiếm full chiều ngang, cao 260px cho cân với donut */
#vsp-chart-trend,
#vsp-chart-tool-crit-high {
  display: block;
  width: 100% !important;
  height: 260px !important;
  max-height: 260px !important;
}

/* Donut vẫn giữ vuông 260x260 như đã chỉnh */
#vsp-chart-severity {
  display: block;
  margin: 0 auto;
  width: 260px !important;
  height: 260px !important;
  max-width: 260px !important;
  max-height: 260px !important;
}

/* Hàng dưới (3 card bảng) – thu gọn để vừa 1 màn 1080p */
.vsp-dashboard-bottom .vsp-card {
  padding-top: 14px;
  padding-bottom: 14px;
}

/* Bảng trong 3 card dưới: chữ nhỏ lại + row thấp hơn */
.vsp-dashboard-bottom table {
  font-size: 11px;
}
.vsp-dashboard-bottom table td,
.vsp-dashboard-bottom table th {
  padding-top: 4px;
  padding-bottom: 4px;
}

/* Nếu vẫn hơi dài, cho phép bảng cuộn nhẹ bên trong card
   nhưng KHÔNG cuộn toàn trang */
.vsp-dashboard-bottom .vsp-table-wrapper {
  max-height: 270px;
  overflow-y: auto;
}

/* Hàng dưới (3 card bảng) – thu gọn để vừa 1 màn 1080p */
.vsp-dashboard-bottom .vsp-card {
  padding-top: 14px;
  padding-bottom: 14px;
}

/* Bảng trong 3 card dưới: chữ nhỏ lại + row thấp hơn */
.vsp-dashboard-bottom table {
  font-size: 11px;
}
.vsp-dashboard-bottom table td,
.vsp-dashboard-bottom table th {
  padding-top: 4px;
  padding-bottom: 4px;
}

/* Nếu vẫn hơi dài, cho phép bảng cuộn nhẹ bên trong card
   nhưng KHÔNG cuộn toàn trang */
.vsp-dashboard-bottom .vsp-table-wrapper {
  max-height: 270px;
  overflow-y: auto;
}

/* Hàng dưới (3 card bảng) – thu gọn để vừa 1 màn 1080p */
.vsp-dashboard-bottom .vsp-card {
  padding-top: 14px;
  padding-bottom: 14px;
}

/* Bảng trong 3 card dưới: chữ nhỏ lại + row thấp hơn */
.vsp-dashboard-bottom table {
  font-size: 11px;
}
.vsp-dashboard-bottom table td,
.vsp-dashboard-bottom table th {
  padding-top: 4px;
  padding-bottom: 4px;
}

/* Nếu vẫn hơi dài, cho phép bảng cuộn nhẹ bên trong card
   nhưng KHÔNG cuộn toàn trang */
.vsp-dashboard-bottom .vsp-table-wrapper {
  max-height: 270px;
  overflow-y: auto;
}

/* ===== 4 TAB VSP (RUNS, DATA, SETTINGS, RULES) ===== */

#tab-runs .vsp-tab-body,
#tab-data .vsp-tab-body,
#tab-settings .vsp-tab-body,
#tab-rules .vsp-tab-body {
  display: flex;
  flex-direction: column;
  gap: 16px;
  height: 100%;
}

#tab-runs .vsp-top-row,
#tab-settings .vsp-top-row {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16px;
}

#tab-runs .vsp-main-row,
#tab-data .vsp-main-row,
#tab-settings .vsp-main-row,
#tab-rules .vsp-main-row {
  flex: 1;
  min-height: 0;
  display: flex;
}

#tab-runs .vsp-main-row .vsp-card,
#tab-data .vsp-main-row .vsp-card,
#tab-settings .vsp-main-row .vsp-card,
#tab-rules .vsp-main-row .vsp-card {
  flex: 1;
  min-height: 0;
}

/* Bảng trong 4 tab – cuộn trong card, không cuộn toàn page */
.vsp-card-table .vsp-table-wrapper {
  height: 100%;
  max-height: 100%;
  overflow-y: auto;
}

/* Bảng: chữ nhỏ hơn dashboard một chút cho gọn */
#tab-runs .vsp-table,
#tab-data .vsp-table,
#tab-rules .vsp-table {
  font-size: 11px;
}
#tab-runs .vsp-table th,
#tab-runs .vsp-table td,
#tab-data .vsp-table th,
#tab-data .vsp-table td,
#tab-rules .vsp-table th,
#tab-rules .vsp-table td {
  padding-top: 4px;
  padding-bottom: 4px;
}

/* Settings – pill tools */
.vsp-tools-pill-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 8px;
}
.vsp-tool-pill {
  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.6);
  font-size: 11px;
}

/* VSP – TAB PANE VISIBILITY */
section[id^="tab-"].tab-pane {
  display: none;
}
section[id^="tab-"].tab-pane.vsp-active {
  display: block;
}

/* ========== DATA SOURCE FILTERS + PAGING ========== */
.vsp-ds-controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  margin: 6px 6px 4px;
  font-size: 12px;
}

.vsp-ds-filters {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.vsp-ds-filters label {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 12px;
}

.vsp-ds-filters select,
.vsp-ds-filters input {
  background: rgba(8, 18, 40, 0.95);
  border: 1px solid rgba(120, 180, 255, 0.35);
  border-radius: 999px;
  padding: 3px 10px;
  color: #e6f3ff;
  font-size: 12px;
  outline: none;
}

.vsp-ds-filters input::placeholder {
  color: rgba(230, 243, 255, 0.4);
}

.vsp-ds-paging {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 12px;
}

.vsp-ds-page-buttons {
  display: flex;
  gap: 4px;
}

.vsp-ds-paging button {
  min-width: 26px;
  border-radius: 999px;
  border: 1px solid rgba(120, 180, 255, 0.4);
  background: rgba(8, 18, 40, 0.9);
  color: #e6f3ff;
  padding: 3px 0;
  cursor: pointer;
}

.vsp-ds-paging button:disabled {
  opacity: 0.4;
  cursor: default;
}

.vsp-ds-paging select {
  background: rgba(8, 18, 40, 0.95);
  border: 1px solid rgba(120, 180, 255, 0.35);
  border-radius: 999px;
  padding: 3px 8px;
  color: #e6f3ff;
  font-size: 12px;
}

/* Hover row cho mọi bảng (Runs, Data Source, Rule Overrides...) */
.vsp-row-hover {
  background: rgba(77, 156, 255, 0.12) !important;
}


/* ===== VSP LAYOUT FULLSCREEN FIX v1 ===== */
html, body {
  height: 100%;
}

body {
  margin: 0;
  padding: 0;
  overflow: hidden; /* Không cuộn toàn trang */
}

/* Vùng shell chính (nếu có các id này thì nó sẽ full-height) */
#vsp-shell,
#vsp-main,
#vsp-main-layout {
  height: 100%;
  display: flex;
  flex-direction: column;
}

/* Hàng chứa tab bar */
#vsp-main-tabs-row {
  flex: 0 0 auto;
  padding: 6px 16px 0;
}

/* Vùng content (tab-pane) */
#vsp-main-tab-content {
  flex: 1 1 auto;
  min-height: 0;
  overflow: hidden;
}

/* Mỗi tab chiếm trọn phần content */
#tab-dashboard,
#tab-runs,
#tab-datasource,
#tab-settings,
#tab-rule-overrides {
  height: 100%;
  padding: 12px 16px 16px;
  box-sizing: border-box;
  overflow: hidden;
}

/* Dashboard: 2 cột – KPI + Chart – co giãn theo chiều cao */
.vsp-dashboard-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.05fr) minmax(0, 1.25fr);
  grid-template-rows: 1fr;
  gap: 12px;
  align-items: stretch;
  height: 100%;
}

.vsp-dashboard-kpi-column {
  display: flex;
  flex-direction: column;
  gap: 8px;
  min-width: 0;
}

.vsp-dashboard-chart-column {
  display: grid;
  grid-template-rows: minmax(0, 1.1fr) minmax(0, 0.9fr);
  gap: 8px;
  min-width: 0;
}

/* Card chung */
.vsp-card {
  display: flex;
  flex-direction: column;
  padding: 10px 12px;
  border-radius: 10px;
  box-sizing: border-box;
  min-height: 0;
}

/* Vùng chứa chart */
.vsp-card-chart {
  flex: 1 1 auto;
  min-height: 0;
}

.vsp-card-chart canvas {
  width: 100% !important;
  height: 100% !important;
}

/* KPI row: fit hơn cho 1080p */
.vsp-kpi-row {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 8px;
}

.vsp-kpi-card {
  padding: 8px 10px;
  border-radius: 8px;
}

/* Runs / DataSource / RuleOverrides: 1 card full-height, table scroll bên trong */
#tab-runs .vsp-fullheight-card,
#tab-datasource .vsp-fullheight-card,
#tab-rule-overrides .vsp-fullheight-card {
  height: 100%;
  display: flex;
  flex-direction: column;
  padding: 10px 12px;
  box-sizing: border-box;
}

/* Header + filter bar của card */
.vsp-card-header-row {
  flex: 0 0 auto;
}

/* Table scroll trong vùng còn lại */
.vsp-card-table-scroll {
  flex: 1 1 auto;
  min-height: 0;
  overflow: auto;
}

/* Tab button nhỏ lại cho đỡ chiếm chỗ */
.vsp-main-tabs button {
  padding: 6px 12px;
  font-size: 13px;
}




/* ===== VSP RUN CENTER CSS v2 ===== */

/* Thanh RUN trên Dashboard */
#vsp-run-bar {
  width: 100%;
  max-width: 1180px;            /* trung tâm, không quá rộng */
  margin: 8px auto 12px auto;   /* cách trên/dưới một chút */
  padding: 8px 12px;
  border-radius: 12px;
  background: linear-gradient(90deg, rgba(20,40,80,0.9), rgba(15,25,60,0.9));
  box-shadow: 0 10px 30px rgba(0,0,0,0.45);
  display: flex;
  flex-wrap: wrap;
  align-items: flex-end;
  justify-content: flex-start;
  gap: 12px;
  box-sizing: border-box;
}

/* Label & input chung */
.vsp-label {
  font-size: 11px;
  letter-spacing: 0.03em;
  text-transform: uppercase;
  opacity: 0.85;
  margin-bottom: 3px;
}

.vsp-input {
  font-size: 13px;
  background: rgba(5, 10, 30, 0.9);
  color: #f7f9ff;
  border: 1px solid rgba(130, 180, 255, 0.3);
  border-radius: 8px;
}

/* Ô RUN SCAN button block */
#vsp-run-button {
  background: linear-gradient(135deg, #28e98c, #1abf6c);
  border: none;
  color: #020617;
  text-transform: uppercase;
  font-size: 12px;
  letter-spacing: 0.08em;
}
#vsp-run-button:hover {
  filter: brightness(1.05);
}

/* Status text nhỏ */
#vsp-run-status,
#vsp-settings-status {
  font-size: 11px;
  opacity: 0.8;
}

/* Block config ở Settings: căn giữa, không dính mép dưới */
#vsp-settings-config-block {
  width: 100%;
  max-width: 1180px;
  margin: 8px auto 12px auto;
  padding: 10px 14px;
  border-radius: 14px;
  background: linear-gradient(90deg, rgba(15,30,70,0.95), rgba(8,14,40,0.95));
  box-shadow: 0 12px 32px rgba(0,0,0,0.45);
  box-sizing: border-box;
}

/* Nút save default */
#vsp-settings-save-btn {
  background: rgba(148, 163, 184, 0.2);
  border: 1px solid rgba(148, 163, 184, 0.6);
  color: #e5e7eb;
  text-transform: uppercase;
  font-size: 11px;
  letter-spacing: 0.09em;
}
#vsp-settings-save-btn:hover {
  background: rgba(148, 163, 184, 0.35);
}

