(function () {
  if (!window.VSP) window.VSP = {};
  window.VSP.initSettings = function () {
    console.log('[VSP][UI] initSettings');
    // TODO: dùng VSP.API.getSettings() để đổ 4 card + bảng tools + profile/integration
  };
})();
