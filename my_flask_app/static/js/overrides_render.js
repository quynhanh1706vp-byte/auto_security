(function () {
  if (!window.VSP) window.VSP = {};
  window.VSP.initOverrides = function () {
    console.log('[VSP][UI] initOverrides');
    // TODO: dùng VSP.API.getRuleOverrides() để đổ bảng overrides + impact preview
  };
})();
