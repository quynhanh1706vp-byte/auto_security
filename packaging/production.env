# VSP UI Gateway - Production Environment
# Location: /etc/vsp-ui/production.env

# Bind
VSP_UI_HOST=127.0.0.1
VSP_UI_PORT=8910

# Service
VSP_UI_SVC=vsp-ui-8910.service

# Base URL used by smoke scripts
VSP_UI_BASE=http://127.0.0.1:8910

# Optional: Gunicorn workers (if using gunicorn)
VSP_UI_WORKERS=2

# NOTE:
# - If you reverse-proxy (Nginx), keep bind at 127.0.0.1 and expose via proxy.
# - Keep /api/healthz and /api/readyz accessible for monitoring.
